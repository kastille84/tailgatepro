# offline todo items

list of offline items to do before app can be pushed to production

- Google Cloud Platform:
  The app is in testing status. We Need to update the Verification Status on your account before production

- mailgun:
  current SMTP login cred has something similar to 'brad@sandbox90ca912a55d3454dbc07e277a317a460.mailgun.org' it's probably because we are using the free account. When going to prod to handle more than 100 messages a day, we must upgrade our account at mailgun.com and update to paid account and create new credentials this time having a name like "ssssomething@gettailgatepro.com"
